# Local dev only: storefront API key (portal -> store -> API keys).
# Copy to .env.local. Do NOT set in production builds — there the store is
# resolved from the site's domain (Origin header).
VITE_STORE_KEY=
